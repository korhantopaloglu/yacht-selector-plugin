# Yacht Selector Plugin

Custom WordPress plugin for rendering a yacht selection interface via shortcode.

## Branching
- Main release branch: `main`
- Working branch: `develop`

## Goal
Keep the frontend aligned with the static reference HTML while making content dynamic through WordPress.